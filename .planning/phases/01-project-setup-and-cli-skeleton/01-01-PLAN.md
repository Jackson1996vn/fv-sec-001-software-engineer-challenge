---
phase: 01-project-setup-and-cli-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - settings.gradle
  - gradle/wrapper/gradle-wrapper.properties
  - gradle/wrapper/gradle-wrapper.jar
  - gradlew
  - gradlew.bat
  - src/main/java/com/campaign/App.java
  - .gitignore
autonomous: true
requirements:
  - CLI-05
must_haves:
  truths:
    - "Running ./gradlew build produces a fat JAR in build/libs/"
    - "Running java -jar build/libs/campaign-csv-analyzer.jar prints output and exits"
  artifacts:
    - path: "build.gradle"
      provides: "Gradle build configuration with shadow plugin, Picocli, and Commons CSV dependencies"
      contains: "com.github.johnrengelman.shadow"
    - path: "settings.gradle"
      provides: "Project name setting"
      contains: "campaign-csv-analyzer"
    - path: "gradlew"
      provides: "Gradle wrapper script"
    - path: "src/main/java/com/campaign/App.java"
      provides: "Minimal entry point class"
      contains: "public static void main"
  key_links:
    - from: "build.gradle"
      to: "src/main/java/com/campaign/App.java"
      via: "mainClass configuration in shadow plugin"
      pattern: "mainClass.*com\\.campaign\\.App"
---

<objective>
Initialize the Gradle project with all dependencies, shadow JAR plugin, and a minimal entry point class.

Purpose: Establish the build infrastructure so that subsequent plans can focus purely on CLI logic.
Output: A working Gradle project that compiles and produces an executable fat JAR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Gradle project with wrapper, dependencies, and shadow plugin</name>
  <read_first>.gitignore, CLAUDE.md</read_first>
  <files>build.gradle, settings.gradle, gradle/wrapper/gradle-wrapper.properties, gradle/wrapper/gradle-wrapper.jar, gradlew, gradlew.bat, .gitignore</files>
  <action>
1. Run `gradle init --type java-application --dsl groovy --test-framework junit-jupiter --java-version 21 --project-name campaign-csv-analyzer --package com.campaign` to scaffold the project. If `gradle` is not available on PATH, download the Gradle wrapper manually by creating the necessary files.

2. Edit `settings.gradle` to contain exactly:
```
rootProject.name = 'campaign-csv-analyzer'
```

3. Replace `build.gradle` with:
```groovy
plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

group = 'com.campaign'
version = '1.0.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'info.picocli:picocli:4.7.6'
    implementation 'org.apache.commons:commons-csv:1.11.0'
    annotationProcessor 'info.picocli:picocli-codegen:4.7.6'

    testImplementation 'org.junit.jupiter:junit-jupiter:5.11.3'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

application {
    mainClass = 'com.campaign.App'
}

shadowJar {
    archiveBaseName.set('campaign-csv-analyzer')
    archiveClassifier.set('')
    archiveVersion.set('')
    mergeServiceFiles()
}

tasks.named('test') {
    useJUnitPlatform()
}
```

4. Update `.gitignore` to include standard Gradle/Java ignores:
```
.planning/
.gradle/
build/
*.class
*.jar
*.log
.idea/
*.iml
.vscode/
.DS_Store
```

5. Verify the Gradle wrapper files exist (`gradlew`, `gradlew.bat`, `gradle/wrapper/gradle-wrapper.properties`, `gradle/wrapper/gradle-wrapper.jar`). If `gradle init` did not produce them, generate via `gradle wrapper --gradle-version 8.10`.
  </action>
  <verify>
    <automated>cd /Users/jackson/Documents/Learning/fv-sec-001-software-engineer-challenge && ./gradlew --version</automated>
  </verify>
  <acceptance_criteria>
    - File `build.gradle` exists and contains the string `com.github.johnrengelman.shadow`
    - File `build.gradle` contains `info.picocli:picocli:4.7.6`
    - File `build.gradle` contains `org.apache.commons:commons-csv:1.11.0`
    - File `build.gradle` contains `mainClass = 'com.campaign.App'`
    - File `settings.gradle` contains `campaign-csv-analyzer`
    - File `gradlew` exists and is executable
    - File `gradle/wrapper/gradle-wrapper.properties` exists
    - `./gradlew --version` exits with code 0
  </acceptance_criteria>
  <done>Gradle project initialized with all dependencies, shadow plugin configured, wrapper functional</done>
</task>

<task type="auto">
  <name>Task 2: Create minimal App entry point and verify fat JAR builds</name>
  <read_first>build.gradle</read_first>
  <files>src/main/java/com/campaign/App.java</files>
  <action>
1. Create directory structure: `src/main/java/com/campaign/`

2. Create `src/main/java/com/campaign/App.java` with this exact content:
```java
package com.campaign;

public class App {
    public static void main(String[] args) {
        System.out.println("Campaign CSV Analyzer - use --help for usage");
    }
}
```

This is a temporary placeholder. Plan 01-02 will replace it with the full Picocli implementation.

3. Run `./gradlew clean shadowJar` to build the fat JAR.

4. Verify: `java -jar build/libs/campaign-csv-analyzer.jar` prints the placeholder message and exits with code 0.
  </action>
  <verify>
    <automated>cd /Users/jackson/Documents/Learning/fv-sec-001-software-engineer-challenge && ./gradlew clean shadowJar && java -jar build/libs/campaign-csv-analyzer.jar</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/java/com/campaign/App.java` exists
    - File `src/main/java/com/campaign/App.java` contains `package com.campaign;`
    - File `src/main/java/com/campaign/App.java` contains `public static void main`
    - `./gradlew clean shadowJar` exits with code 0
    - File `build/libs/campaign-csv-analyzer.jar` exists after build
    - `java -jar build/libs/campaign-csv-analyzer.jar` exits with code 0
  </acceptance_criteria>
  <done>Fat JAR builds successfully and is executable via java -jar</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only creates build infrastructure; no user input processing yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | build.gradle dependencies | accept | Dependencies are from Maven Central; version-pinned. Low risk for a CLI tool. |
</threat_model>

<verification>
1. `./gradlew clean shadowJar` completes without errors
2. `java -jar build/libs/campaign-csv-analyzer.jar` runs and exits cleanly
3. All dependencies resolve (picocli, commons-csv)
</verification>

<success_criteria>
- Gradle wrapper is functional
- Shadow JAR plugin produces campaign-csv-analyzer.jar
- JAR is executable and runs without errors
- All Phase 2+ dependencies (commons-csv) are already declared
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-and-cli-skeleton/01-01-SUMMARY.md`
</output>
